# Быстрый старт SEO-парсера

## 🚀 Самый простой способ (одна команда)

Запустите анализ на вашем VPN-сервере:

```bash
./run_on_server.sh root@your-server.com
```

## 📋 Что происходит

1. Скрипт подключается к вашему VPN-серверу по SSH
2. Устанавливает Python и все зависимости
3. Создает SEO-парсер
4. Запускает анализ 30 стран
5. Показывает результаты в консоли

## 🔑 Предварительная настройка (для автоматического входа)

Чтобы не вводить пароль:

```bash
# Генерация SSH ключа (если нет)
ssh-keygen -t ed25519

# Копирование ключа на сервер
ssh-copy-id root@your-server.com
```

## 🔄 Повторный запуск (без установки)

После первого раза можно запускать просто:

```bash
./run_on_server.sh root@your-server.com
```

Или напрямую через SSH:

```bash
ssh root@your-server.com 'cd ~/seo-parser && source venv/bin/activate && python main.py'
```

## 📊 Что вы получите

- **ТОП-20 стран** по спросу на VPN
- **Страны с растущим спросом** (тренды)
- **Рекомендации** какие серверы добавить в первую очередь
- Детали по каждой стране и типам запросов

## ⚠️ Почему запускать на сервере?

Google блокирует запросы из России (ошибка 429). На VPN-сервере анализ пройдет без проблем.

## 🛠️ Локальный запуск (для тестов)

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py
```

⚠️ Внимание: Google может заблокировать запросы из России.

## 📚 Полная документация

Смотрите [README.md](README.md) для подробной информации.