# SEO-парсер для анализа спроса на VPN по локациям

Анализирует поисковые запросы в Google Trends, чтобы определить в какие страны нужно добавить VPN-серверы.

**⚡ Быстрый запуск на VPN-сервере:**
```bash
./run_on_server.sh root@your-server.com
```

**⚠️ Важно:** Google блокирует запросы из России. Рекомендуется запускать парсер на VPN-сервере.

## Установка

1. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

## Использование

### ⚡ Быстрый запуск на удаленном VPN-сервере (рекомендуется)

Этот способ автоматически развернет и запустит парсер на вашем сервере:

```bash
./run_on_server.sh root@your-server.com
```

Скрипт:
1. Подключится к серверу по SSH
2. Установит Python и зависимости
3. Развернет SEO-парсер
4. Запустит анализ
5. Покажет результаты

**Повторный запуск без развертывания:**
```bash
ssh root@your-server.com 'cd ~/seo-parser && source venv/bin/activate && python main.py'
```

### Локальный запуск (для тестирования)

#### Запуск полного анализа

```bash
python main.py
```

**⚠️ Ограничение:** Google часто блокирует запросы из России (ошибка 429). Для полноценной работы используйте VPN или запускайте на удаленном сервере.

Анализ будет включать:
- 30 стран с популярными VPN-локациями
- 2 периода: 1 месяц и 3 месяца
- 8 вариаций запросов для каждой страны (на русском и английском)

### Запуск отдельных компонентов

**Тест генератора запросов:**
```bash
python query_builder.py
```

**Тест парсера Google Trends:**
```bash
python google_trends_parser.py
```

**Тест анализатора:**
```bash
python analyzer.py
```

## Структура проекта

```
seo-parser/
├── requirements.txt              # Зависимости
├── config.py                    # Конфигурация стран и настроек
├── query_builder.py             # Генератор вариаций запросов
├── google_trends_parser.py      # Парсер Google Trends
├── analyzer.py                  # Анализатор данных
├── main.py                      # Главный файл для запуска
└── README.md                    # Этот файл
```

## Что анализируется

### Вариации запросов для каждой страны

- Русский: "впн [страна]", "[страна] впн", "[адъектив] впн", "впн для [страны]"
- Английский: "vpn [country]", "[country] vpn", "[adjective] vpn", "vpn for [country]"

Пример для Турции:
- турецкий впн
- впн турция
- турция впн
- впн для турции
- turkish vpn
- turkey vpn
- turkey vpn
- vpn for turkey

### Периоды анализа

- 1 месяц (текущие тренды)
- 3 месяца (среднесрочные тренды)

### Метрики

- Популярность запросов (0-100)
- Топ запрос для страны
- Сравнение периодов (рост/падение)
- Связанные запросы
- Рекомендации по приоритету добавления серверов

## Пример вывода

```
================================================================================
                    АНАЛИЗ СПРОСА НА VPN ПО ЛОКАЦИЯМ
                              Россия | Google Trends
================================================================================

Генерация поисковых запросов...
✓ Сгенерировано 30 стран с 240 вариациями запросов

Инициализация парсера Google Trends...
✓ Парсер готов

============================================================
Начинаем парсинг 30 стран...
============================================================
[1/30] Парсим Турция...
[2/30] Парсим Казахстан...
...

================================================================================
ТОП-20 СТРАН ПО СПРОСУ (3 месяца)
================================================================================
№   Страна              Популярность   Топ запрос
--------------------------------------------------------------------------------
1   Турция              95             турецкий впн
2   Казахстан           88             казахский впн
3   Германия            72             немецкий впн
...

================================================================================
РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТУ ДОБАВЛЕНИЯ СЕРВЕРОВ
================================================================================

🔥 КРИТИЧЕСКИЙ ПРИОРИТЕТ (высокий спрос + рост):
  • Турция               (спрос: 95, рост: +3.2%)

✅ ВЫСОКИЙ ПРИОРИТЕТ (высокий спрос):
  • Казахстан            (спрос: 88)
  • Германия             (спрос: 72)
```

## Кастомизация

### Добавление новых стран

Отредактируйте файл `config.py`:

```python
COUNTRIES = {
    "Новая страна": {
        "name_en": "New Country",
        "adjective_ru": "новый",
        "adjective_en": "new"
    },
    # ... остальные страны
}
```

### Изменение периодов

В файле `config.py`:

```python
TIMEFRAMES = {
    "1_month": "today 1-m",
    "3_months": "today 3-m",
    "1_year": "today 12-m"  # добавить год
}
```

### Изменение геолокации

Чтобы анализировать запросы из другой страны, измените в `config.py`:

```python
GEO = "US"  # для США
# или
GEO = "DE"  # для Германии
```

## Установка на VPN-сервере

### Автоматическая установка (рекомендуется)

```bash
./run_on_server.sh root@your-server.com
```

### Ручная установка

Подключитесь к серверу и выполните:

```bash
# Копируем скрипт развертывания на сервер
scp deploy.sh root@your-server.com:~/

# Подключаемся к серверу
ssh root@your-server.com

# Запускаем развертывание
bash ~/deploy.sh

# Запускаем анализ
cd ~/seo-parser
source venv/bin/activate
python main.py
```

### Предварительная настройка SSH (для автоматического входа)

Чтобы не вводить пароль каждый раз, добавьте свой SSH ключ на сервер:

```bash
# Генерация ключа (если нет)
ssh-keygen -t ed25519

# Копирование ключа на сервер
ssh-copy-id root@your-server.com

# Теперь можно подключаться без пароля
ssh root@your-server.com
```

## Ограничения

- **Google Trends**: Относительная популярность (0-100), а не точное количество запросов
- **Лимиты запросов**: Между запросами есть задержки (2 секунды по умолчанию)
- **Точность**: Данные из Google Trends могут быть округлены
- **Блокировки**: Google временно блокирует запросы из России (ошибка 429). **Решение: запускайте на VPN-сервере**

## Решение проблем

### Ошибка 429 (Too Many Requests) - Google блокирует запросы

**Причина:** Google временно блокирует запросы из России.

**Решения:**
1. **Рекомендуется:** Запустите парсер на VPN-сервере:
   ```bash
   ./run_on_server.sh root@your-server.com
   ```

2. **Альтернатива:** Увеличьте задержку между запросами в `config.py`:
   ```python
   REQUEST_DELAY = 5  # увеличить до 5 секунд
   ```

3. **Альтернатива:** Используйте VPN при локальном запуске

### Нет данных для страны

Google Trends может не иметь данных для некоторых комбинаций запросов и регионов. Это нормально.

### Ошибка подключения к серверу

Если скрипт `run_on_server.sh` не может подключиться:

1. Проверьте, что сервер доступен:
   ```bash
   ping your-server.com
   ```

2. Убедитесь, что порт SSH открыт (по умолчанию 22)

3. Добавьте SSH ключ для автоматического входа:
   ```bash
   ssh-copy-id root@your-server.com
   ```

4. Или введите пароль вручную при запросе

## Лицензия

MIT

## Структура проекта

```
seo-parser/
├── requirements.txt              # Зависимости
├── config.py                    # Конфигурация стран и настроек
├── query_builder.py             # Генератор вариаций запросов
├── google_trends_parser.py      # Парсер Google Trends
├── analyzer.py                  # Анализатор данных
├── main.py                      # Главный файл для запуска
├── deploy.sh                   # Скрипт развертывания на сервере
├── run_on_server.sh            # Скрипт запуска через SSH
└── README.md                    # Этот файл
```

## Скрипты для работы с сервером

### deploy.sh

Скрипт для автоматического развертывания на Ubuntu/Debian сервере:

- Обновляет систему
- Устанавливает Python 3 и pip
- Создает виртуальное окружение
- Создает все файлы проекта
- Устанавливает зависимости

Запуск на сервере:
```bash
bash deploy.sh
```

### run_on_server.sh

Скрипт для запуска с локального компьютера:

- Проверяет SSH подключение
- Копирует скрипт развертывания
- Запускает развертывание
- Запускает анализ
- Показывает результаты

Запуск с локального компьютера:
```bash
./run_on_server.sh root@your-server.com
```

## Технологии

- Python 3.x
- pytrends - библиотека для Google Trends API
- pandas - обработка данных
- requests - HTTP запросы
- Bash - скрипты автоматизации
